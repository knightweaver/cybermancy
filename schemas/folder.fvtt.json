{
  "$id": "folder.fvtt.json",
  "type": "object",
  "required": ["name", "type"],
  "properties": {
    "name": { "type": "string" },
    "type": { "enum": ["Item", "Actor", "JournalEntry", "RollTable"] },
    "folder": { "type": ["string", "null"] },
    "sorting": { "type": "string", "enum": ["a", "m"] }
  }
}
